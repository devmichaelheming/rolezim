import React, { FC, ReactElement } from "react";

import EventDetail from "~/components/Entidades/EventDetail";

const EventDetailSreen: FC = (): ReactElement => {
  return <EventDetail />;
};

export default EventDetailSreen;
