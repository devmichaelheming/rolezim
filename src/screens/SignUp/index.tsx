import React, { FC, ReactElement } from "react";

import SignUp from "~/components/Entidades/SignUp";

const SignUpScreen: FC = (): ReactElement => {
  return <SignUp />;
};

export default SignUpScreen;
