import React, { FC, ReactElement } from "react";

import Favorite from "~/components/Entidades/Favorite";

const FavoriteScreen: FC = (): ReactElement => {
  return <Favorite />;
};

export default FavoriteScreen;
