import React, { FC, ReactElement } from "react";

import SignIn from "~/components/Entidades/SignIn";

const SignInScreen: FC = (): ReactElement => {
  return <SignIn />;
};

export default SignInScreen;
